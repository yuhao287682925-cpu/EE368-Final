# Kinova Gen3-lite 接触式力控绘制系统 (EE368-Final)

本项目旨在 Kinova Gen3-lite (6 DOF) 协作机器人上实现末端 Z 轴自适应绘制。针对 Gen3-lite 基于电流估算力矩带来的“姿态假力”问题，系统创新性地采用了**“定深位置控制保底 + 单向安全泄压阀”**的融合控制策略。在保证轨迹连续稳定、不出现误判抬笔悬空的前提下，有效解决了在粗糙纸箱表面绘制时遭遇鼓包卡阻的问题。

---

## 📁 目录与核心脚本说明

### 1. 核心控制与测试脚本 (`tools/`)

* **`auto_contact_draw.py`**：**顶面绘制主程序（当前最优极简版本）**。包含自动寻面与原点对齐。突破性地引入了 **“直接关节转速逆解控制”**，绕过原生笛卡尔限制，实现笔尖直接刚性伺服。下探时使用 **“1D 阻抗软着陆”** 实现平滑寻面并精准锁定。绘制期间，采用 **“运动学啄木鸟 (Kinematic Woodpecker) 避障”**，利用刚性转速特性，一旦物理碰壁导致末端失速立刻瞬发 3.5mm 抬升泄压，如主动悬挂般完美贴合纸盒坑洼表面。
* **`side_contact_draw.py`**：**垂直侧面绘制主程序**。支持自动寻面（对齐 +Y 面），并将二维轨迹重映射至垂直立面（X-Z 平面），实现了取反重力的向上绘制防卡死逻辑。包含针对侧面动摩擦对抗的定制化力控与退缩策略。
* **`side_contact_draw_with_log.py`**：**带日志与误差分析的侧边绘制程序**。除了执行侧向绘制外，以 40Hz 高频记录底层物理轨迹。结合引入了积分项（PI控制）修正单向摩擦带来的稳态滞后。执行完毕后会自动触发误差对比程序。
* **`analyze_error.py`**：**误差对比与三维可视化分析工具**。能够提取理论目标点与实际记录点，计算平移均方根误差 (RMSE)，并自动通过方差推断平面生成 1:1 的无失真 2D 投影叠加图及 3D 轨迹走势对比图。
* **`align_wrist_side.py`**：**侧边位姿初始化脚本**。用于强行将六轴机械臂腕部旋转至完全水平指向侧壁的状态。
* **`auto_contact_draw_v1_backup.py`**：**纯动态 PID 力控版本备份**。适用于平整硬桌面等不会出现姿态“假力”干扰的理想环境。
* **`force_z_monitor.py`**：**Z 轴估计力实时监控工具**。支持开机 1.5s 自动去皮，以及在终端中随时按 **[Enter 键]** 在线重新校准零偏。
* **`torque_watchdog.py` / `safety_watchdog.py`**：**安全看门狗**。实时监控关节扭矩和末端受力，超限紧急刹停。
* **`old/`**：废弃/历史版本代码存档目录（不参与 Git 跟踪）。

### 2. 轨迹生成与绘图算法 (`trajectory/`)

* **`cube_circle_trajectory.py`**：生成用于测试绘制的几何轨迹（如圆形、星形、正方形等），保存为 `.csv` 航点文件。
  * **操作方法**：直接在终端运行 `python trajectory/cube_circle_trajectory.py`，程序会自动生成包含目标坐标和法向的各种形状航点文件（如 `star_8cm.csv`）。
* **`plot_trajectory_3d.py`**：三维空间下的轨迹可视化工具。

---

## 💻 虚拟机 (ROS 运行端) 标准操作流程

### 第一步：启动 Kortex 驱动与 ROS 核心

在虚拟机终端中，启动 Kinova 驱动：

```bash
source ~/catkin_workspace/devel/setup.bash
roslaunch kortex_driver kortex_driver.launch arm:="gen3_lite" ip_address:="192.168.1.10"
```

> ⚠️ **注意**：启动后，请将机械臂移动至**起始悬空位置**（笔尖距离表面 2~5cm，并确保不受外力）。

### 第二步：(可选) 实时受力监视与校准 (新终端)

若想单独排查传感器温偏、观察手触笔尖时的压力估计是否准确：

```bash
python3 tools/force_z_monitor.py
```
*(测试过程中，如出现发热漂移，可在该终端按 **[Enter 键]** 随时在线归零。)*

### 第三步：执行自动对刀与定深绘制 (新终端)

我们推荐使用带有日志与误差分析的最新 `_with_log` 脚本。传递轨迹 CSV 文件，启动相应主程序：

**选项 A：在水平桌面/顶面绘制**
```bash
python3 tools/auto_contact_draw_with_log.py trajectory/star_8cm.csv
```

**选项 B：在垂直侧面（+Y法向）绘制**
```bash
# 必须先执行姿态对齐，强制转动手腕水平朝向 +Y
python3 tools/align_wrist_side.py
# 姿态就绪后，执行侧面主程序
python3 tools/side_contact_draw_with_log.py trajectory/star_8cm.csv
```

主程序将自动执行以下操作：

1. **第一阶段：快速探面**：以中速向下探测直至触发 12N 强接触，随后立即抬升 5mm 脱离。
2. **第二阶段：轻柔对刀与校准**：以极慢速度 (3mm/s) 再次下探，触发 1.2N 轻微接触即刹停。以此点作为高精度 Z 轴原点 (`z_nominal`)，并执行 1.5 秒的静态力矩去皮校零。
3. **第三阶段：分离式力控绘制 (Decoupled Force-Position Control)**：
   - **水平移动时锁定Z轴**：在笔尖沿 X-Y 平面拖动绘制时，强制指令 `linear_z = 0`。此举彻底切断了“横向动摩擦力”向“垂直姿态假力”的耦合污染，避免了画笔出现高频的“点头”或异常悬空。
   - **停顿态静力补偿 (Static Force-Offset)**：移动过程中阶段性（每两个航点）插入几十毫秒的极短停顿。利用静止状态下纯净的 Z 轴测力进行逻辑判断：若静压 > 5N，则累加 0.4mm 的安全释压上抬；若 < 2.5N（悬空风险），则主动下压追踪。累计修正量受限幅保护，完美平衡了“贴合起伏表面”与“防止失控卡死”。

---

## ⚙️ 核心参数微调与建议

如需根据不同的笔触、画笔弹性或纸箱硬度微调表现，可在 `tools/auto_contact_draw.py` 中修改：

| 核心控制参数 | 默认值 | 作用说明 |
| :--- | :--- | :--- |
| `max_z_offset` | `+0.0008` (m) | 全局最大允许的上抬释压上限（0.8mm），防止因力矩估算误差导致的彻底悬空。 |
| `min_z_offset` | `-0.010` (m) | 遇到纸箱塌陷时的最大下探追踪深度（10mm）。 |
| 释压微调步长 | `+0.0004` (m) | 停顿期单次力过大时的上抬补偿量。 |
| 巡航位置刚度 `k_pos` | `1.5` | 绘制状态下 XY 轴向目标点逼近的速度比例增益。 |

# 项目任务清单：三维物块表面几何映射绘画系统

基于已确认的 13 天实施方案，项目任务被拆解为以下阶段。各成员可随时根据进度更新此文档。

## 第一阶段：基础搭建与理论推导（Day 1 - Day 3）
- `[ ]` **理论推导与数学建模 (成员A)**
  - `[ ]` 推导 D-H 参数方程，建立基准坐标系到物块局部坐标系(S)的齐次变换映射。
  - `[ ]` 编写 Python 辅助计算代码，通过给定几个特殊点测试数学模型的投影正确性。
- `[ ]` **软件环境部署与基础控制 (成员B)**
  - `[ ]` 配置 Ubuntu + ROS Noetic 工作空间环境。
  - `[ ]` 下载、编译 Kinova Gen3 lite 开源控制包并跑通基本通讯。
  - `[ ]` 编写脚本实现基础的末端笛卡尔空间点到点移动指令。
- `[ ]` **硬件就绪与安全性测试 (成员C)**
  - `[ ]` 完成末端画笔夹具的设计、3D打印与牢固安装。
  - `[ ]` 机器上电调试，测试机械臂拖动示教，并在控制节点中设置软限位（防止碰坏桌面与物块）。

## 第二阶段：核心算法开发与单面测试（Day 4 - Day 7）
- `[ ]` **2D轮廓提取与三维轨迹生成 (成员A)**
  - `[ ]` 编写 Python 脚本解析输入的二维图形（或预设轨迹），完成二维离散化与等距采样。
  - `[ ]` 调用第一阶段公式，将二维散点云投影到基座三维坐标系中。
  - `[ ]` 对离散的三维点序列进行样条插值，生成平滑轨迹点集。
- `[ ]` **逆解接口封装与姿态对齐 (成员B)**
  - `[ ]` 封装开源运动学(IK)接口，使得系统能直接接收生成的三维轨迹点序列。
  - `[ ]` 添加姿态约束逻辑：确保规划出的路径点中，机械臂末端(笔尖)的法向始终垂直于目标绘制面。
- `[ ]` **单面绘图闭环联调 (成员C)**
  - `[ ]` 在单一水平面/斜面上进行基础闭环绘图测试。
  - `[ ]` 测量试画图形的尺寸误差，观察运笔平滑度，向A/B反馈以调整插值密度与运行速度。

## 第三阶段：换面逻辑与多面集成（Day 8 - Day 10）
- `[ ]` **跨面状态机控制逻辑 (成员A & 成员B)**
  - `[ ]` 编写状态机节点，严格实现流程循环：`DRAW → LIFT → ROTATE → MOVE → LOWER → DRAW`。
  - `[ ]` 重点开发抬笔（`LIFT`）与下笔（`LOWER`）动作：确保末端执行器严格沿着当前绘制面的法向直线上升/下降。
- `[ ]` **基础碰撞检测与安全干预 (成员B)**
  - `[ ]` 在整体路径规划前，加入针对已知物块体积的简易碰撞检测。
  - `[ ]` 若检测到 `MOVE` 阶段可能发生干涉，自动抬高移动平面或在途径点间降速。
- `[ ]` **多面切换实机联调 (成员C)**
  - `[ ]` 实机运行跨面绘制，紧盯紧急停止按钮，验证换面时的 `ROTATE` 与 `MOVE` 动作安全且无干涉。
  - `[ ]` 评估法向姿态在切换过程中的过渡平滑性。

## 第四阶段：实机优化、数据分析与结题（Day 11 - Day 13）
- `[ ]` **综合测试与控制微调 (全员)**
  - `[ ]` 开展多组不同姿态立体物块的完整画作连续测试。
  - `[ ]` 针对断触现象调整物块位置标定参数；针对抖动现象优化控制发送频率。
- `[ ]` **数据统计与交付物总结 (全员)**
  - `[ ]` 成员C：录制最终成功的多面连续绘图演示视频，汇总并计算整体尺寸精度与误差分布。
  - `[ ]` 成员A/B：整理代码仓库，撰写 README 并在代码中添加必要注释。
  - `[ ]` 全员：根据开发过程与实机数据，合作撰写最终《三维物块表面几何映射绘画系统》技术结题报告，并制作最终答辩 PPT。

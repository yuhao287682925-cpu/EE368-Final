# EE386 期末项目技术报告：三维物块表面几何映射绘画系统

## 1. 项目概述
### 1.1 项目背景与目标
本项目基于 Kinova Gen3 lite 机械臂，开发了一套能够自动感知并映射复杂物块表面三维位姿，并在其多个面上连续进行二维图形绘制的系统。

### 1.2 团队分工
*   **成员 A**：核心数学建模（坐标映射、D-H矩阵）、轨迹离散化与插值算法。
*   **成员 B**：ROS Noetic 系统集成、状态机逻辑开发、避障与轨迹追踪控制。
*   **成员 C**：硬件系统标定（TCP测量）、数据采集与分析系统搭建、实机闭环联调与性能评估。

---

## 2. 系统设计与理论模型
### 2.1 运动学数学模型 (成员A负责完善)
（此处将写入：基座坐标系到物块局部坐标系的齐次变换矩阵推导，以及逆向运动学的应用机制。）

### 2.2 状态机控制流 (成员B负责完善)
（此处将写入：`DRAW → LIFT → ROTATE → MOVE → LOWER` 换面逻辑的详细设计，以及碰撞检测算法。）

---

## 3. 硬件部署与物理标定机制 (核心由成员C撰写)
### 3.1 相对误差标定法
为了将理想的数学平面映射到物理世界，我们开发了基于实机触摸的标定反馈系统。
通过机械臂末端在物理表面获取 3 个基准点，利用向量叉乘方程 $\vec{n} = \vec{v_1} \times \vec{v_2}$，实时反解出物体各表面的真实法向量与中心点。这彻底消除了机械安装和物体摆放带来的系统级误差。

### 3.2 TCP 偏移量与被动柔顺设计
在实机测试初期，我们发现刚性接触会导致底盘 PID 出现剧烈的高频振荡（Z轴误差呈锯齿状）。
**解决方案**：
1. 我们测量了夹具法兰盘到笔尖的绝对偏移量，并在 ROS 中配置了精确的 `Tool Center Point (TCP)`。
2. 引入了**被动柔顺设计**（在画笔后方增加了弹簧/海绵缓冲）。这一物理改造极大地吸收了绘制平面的微小不平整带来的刚性冲击，将轨迹抖动降低了 XX%。

---

## 4. 实验结果与误差分析
### 4.1 数据获取机制
我们开发了独立的底层状态监听节点，以 40Hz 的频率无死角抓取 Kinova 驱动器返回的真实笛卡尔位姿（`BaseCyclic_Feedback`），确保了实验数据的绝对真实性。

### 4.2 轨迹跟踪误差 (RMSE) 分析
利用 3D 轨迹重构可视化工具，我们将理论数学轨迹与实际执行轨迹进行了对比。

| 实验场景 | 平均绘制速度 | Z轴下垂静态误差 | RMSE (均方根误差) | 改善措施 |
| :--- | :---: | :---: | :---: | :--- |
| **单面水平绘图 (刚性连接)** | 10% | ~1.0 mm | [待填入数据] mm | 增加笔尖柔顺性缓冲 |
| **单面水平绘图 (柔顺笔尖)** | 20% | ~0.2 mm | [待填入数据] mm | - |
| **多面连续绘图 (换面测试)** | 20% | [待填] | [待填入数据] mm | 优化 ROTATE 时的插值步长 |

*(此处可插入 Figure_1 等 matplotlib 生成的 3D 对比图)*

---

## 5. 总结与展望
本系统成功实现了……（总结项目成果）。
未来可进一步结合外部视觉系统（如 RealSense 相机）实现物块位姿的全自动识别。
